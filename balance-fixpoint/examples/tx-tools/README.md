# Cardano Tx Tools native hooks example

This standalone package is built by the repository flake against the exact
pinned Cardano Tx Tools source. For a local Cabal build, run:

```sh
cabal build tx-tools-native-hooks -O0
cabal run tx-tools-native-hooks -O0
```
